# Permutation With Visit Boolean Array

A Permutation method that utilizes recursion and a "visit" boolean array that tracks which numbers have been already used to show all the possible combinations.
